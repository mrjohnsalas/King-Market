@model IEnumerable<King_Market.Models.ClassDocumentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm())
{
    <p>
        Find: @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}

<table class="table">
    <tr>
        <th>
            @Html.ActionLink("Name", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClassDocumentTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClassDocumentTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClassDocumentTypeId })
        </td>
    </tr>
}

</table>
